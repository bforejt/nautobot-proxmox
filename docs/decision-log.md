# Decision Log

Running log of architectural and process decisions. Status values: **Decided** (confirmed
by the team), **Confirmed** (fact established about our environment), **Proposed**
(recommended in the plan, awaiting sign-off), **Open** (needs an answer; see
[plan-of-attack.md §6](plan-of-attack.md) for the full tagged list).

| # | Date | Decision | Status | Notes |
|---|------|----------|--------|-------|
| 1 | 2026-08-05 | Nautobot platform: build on existing **2.4 LTM**; upgrade to 3.x as its own effort, targeted before Phase 4 | **Decided** | Pin 2.4-LTM app trains; isolate approval gating (`approval_required` → Approval Workflows at 3.0); keep libraries pure-Python. Risk: 2.4's Django 4.2 passed upstream EOL Apr 2026; LTM window ends when Nautobot 3.3 ships. |
| 2 | 2026-08-05 | PA-VM management: **standalone or SCM only — no Panorama** | **Decided** | `pa_bootstrap` builder ships two init-cfg variants: `standalone` (auth-code licensing) and `scm` (`panorama-server=cloud` + auto-registration PINs). Per-site choice is a VM attribute, not a code branch. |
| 3 | 2026-08-05 | SE350 fleet is the **"with Security Pack"** variant | **Confirmed** | ThinkShield activation is already required on our units (only Security Pack units have it). RMA runbook starts with ThinkShield claim; pre-ship procedure must handle motion-detection/SED tamper lock. Verify what the current ESXi-era ship process already does about motion detection. |
| 4 | 2026-08-06 | XCC licenses are **Enterprise fleet-wide** | **Confirmed** | The no-USB bare-metal track is unblocked. 2026-08-07: first discovery-job run confirmed **EXT1–EXT4 present** on a real unit at fleet firmware (read side verified). Remaining: the mount/eject write test and the dress-rehearsal boot on a lab unit. |
| 5 | 2026-08-08 | Standalone Proxmox nodes, **never corosync-clustered**; pairing modeled in Nautobot only | **Decided** | Two-node cluster without shared storage loses quorum when one node dies and blocks VM autostart — the exact failure an unattended edge site can't tolerate. One-way: can't join an in-service node with VMs into a cluster later. |
| 6 | — | One LACP bond (802.3ad, xmit-hash layer2+3) + one **VLAN-aware bridge**; no OVS, no SDN zones, no bridge-per-VLAN | Proposed | `tag=` per vNIC = access port; untagged or `trunks=` = VGT-style trunk for self-tagging VNFs (PA-VM, C8000v). Pair hash with Cisco `src-dst-ip` (9300 default `src-mac` polarizes NFV traffic — change it). |
| 7 | 2026-08-08 | Intent/deploy split — **intent side delegated**: a separate layout process (NtC Design Builder App or equivalent) creates all site intent in Nautobot (IPAM carve, devices, Hosted On roster, per-device specifics); this repo's layered idempotent jobs are pure SoT **consumers** that converge infrastructure to it | **Decided** | Collapses all three operating scenarios into one code path entered at different layers. Progress checkpointed in a `provisioning_state` custom field; every layer job re-checks state on entry. |
| 8 | 2026-08-08 | **VNFs modeled as Devices — the team's existing convention; no virtualization model at all.** 0U virtual DeviceTypes (VM-Series, C8000v, C9800-CL, Ubuntu Jump Host VM), existing Firewall role + new Hypervisor/Jump Host roles, placement via a custom one-to-many Relationship **"Hosted On"** (hypervisor Device → VNF Devices), sizing as role standards in profiles (CF overrides for exceptions), `vmid` + `provisioning_state` custom fields. Supersedes the one-host-Cluster idea — with no VM records, no Clusters exist at all | **Decided** | Extensibility objects (Relationships/CFs/Roles/DeviceTypes) are DATA, not schema — created idempotently by the `BootstrapNfvSchema` job, so no Nautobot App is required; App graduation triggers unchanged (custom models/UI/pinned deps). Bonus: Golden Config/Controller stay available (Device-only features). Future SSoT adapter maps to Devices (custom adapter was planned regardless). |
| 9 | — | Firstboot hook builds the **final** network topology (bond + bridge + mgmt); the L2 job is verify/converge only | Proposed | Avoids the strand-prone installer-network→bond cutover where the API call's own transport drops mid-apply. |
| 10 | — | Golden images: HTTPS repo + `download-url` (checksummed, content=`import`) + `import-from` volume IDs; no shared storage, no per-node template maintenance | Proposed | Absolute-path `import-from` fails for all API tokens — the volume-ID path is the only token-compatible mechanism. |
| 11 | 2026-08-09 | Bare-metal ISO strategy: **one generic auto-install ISO + HTTP answer fetch** (answer service renders `answer.toml` from Nautobot, keyed on the installer's identity POST) | **Decided** | Verified (Aug 2026) against installer source + wiki: the answer request POSTs DMI system/baseboard/chassis serials, UUID, product name, and every NIC's link+MAC — per-node server-side answers are the documented, supported pattern (PVE 9.2 adds a bearer `--answer-auth-token`). Discovery mode pairs with delivery (#41): vmedia/field → baked `--url` + cert fingerprint (no site-DHCP dependency); PXE/lab → DHCP opt 250 or DNS TXT. XCC1 constraint stands: the vmedia ISO URL itself must be plain HTTP. |
| 12 | — | Field bare-metal reinstalls over WAN: **never** — always ship a lab-rebuilt unit; field operations are VM-level only (L3/L4), approval-gated | Proposed | Reversing this materially changes the answer-service/webhook connectivity and TLS design. |
| 13 | — | Hypervisor pairs stay **homogeneous** during ESXi→Proxmox cutover (a failure-driven migration converts both nodes of a pair) | Proposed | A mixed ESXi+Proxmox pair means two toolchains for one redundant VM pair mid-incident. Cost this into the cutover policy. |
| 14 | — | Platform profiles as data (per-DeviceType YAML: vmedia method, BIOS attribute map, disk filter, NIC naming, licenses) | Proposed | SE350 is XCC1 (PATCH-on-EXT vmedia); SE455 V3 is XCC2 (POST InsertMedia); fleet will be mixed — no per-platform if/else in code. |
| 15 | 2026-08-06 | LACP (`mode active`) is the server-facing standard. Net-new sites (primary case): configured from day one in the switch template. Conversions (secondary): flip `mode on` → `mode active` in the same window as the server conversion | **Decided** | Team agreed. Mode-on existed only because ESXi standard vSwitches can't LACP. Both mismatch directions black-hole (802.3ad↔mode-on = partial hash-dependent loss; balance-xor↔mode-active = members suspended). Verify post-flip: `show etherchannel summary` flags `P`; bond partner MAC non-zero. See [site-reference-architecture.md](site-reference-architecture.md). |
| 16 | 2026-08-06 | Image/firmware repo = existing **nautobot-composer** server | **Decided** | Serves versioned qcow2/ISO + SHA256 sidecars over HTTPS for node `download-url` pulls; needs a plain-HTTP vhost for XCC1 install-ISO mounts. The answer service remains the only net-new infrastructure component. |
| 17 | 2026-08-06 | Fleet hardware is uniform: SE350, 16-core Xeon D-2183IT, 256 GB RAM | **Confirmed** | Neutralizes the PA-VM CPUID licensing concern for RMA swaps (same-SKU is automatic) and fixes the guardrail actuals. |
| 18 | 2026-08-06 | Oversubscription policy: used CPU/RAM must not exceed actuals; storage thin-provisioning allowed; "actuals" = **physical cores, not SMT threads** | **Confirmed** (policy) / Proposed (core-counting interpretation) | Budget: 16 cores − 2–4 host-reserved ≈ 12–14 VNF vCPUs; RAM 256 GB − host overhead (no ZFS ARC, per #27). Encoded in SiteNfvDesignJob (intent-time) + DeployVmJob (deploy-time) guardrails. |
| 19 | 2026-08-06 | Café-model VLAN standard: 200 mgmt / 210 IoT / 220 users + 904 DIA2 / 905 DIA1 / 907 PA-HA; L2-only switches, SVIs on the PA HA pair | **Confirmed** | Seeds the design job's IPAM/VLAN templates. Per-site naming drift exists in the legacy estate — design job emits standard names; audit job flags drift. VLAN 907 must be in `bridge_vids`/trunks even though it carries no user traffic. |
| 20 | 2026-08-06 | Port roles **confirmed**: vSwitch0/mgmt = p3/p4 copper, active/passive (no port-channel) into **access ports**; LAN-Trunk/data = f1/f2 10G fiber, channeled. Proxmox mirror: `vmbr0` = active-backup bond on copper (untagged) + `vmbr1` = VLAN-aware bridge on the f1/f2 802.3ad bond, MTU 9000 target on the data path | **Confirmed** (wiring) / Decided (mapping) | Bonus: the LACP flip touches only the data pair — mgmt switch config is unchanged at conversion, so the mgmt path is never at risk from the channel-mode change. Remaining lab-verify: Linux NIC-name pinning map (PCI path per port) and the switch jumbo question (#25). |
| 21 | 2026-08-06 | Legacy VMs run `sched.cpu.latencysensitivity=high` + CPU/memory reservations. KVM has **no reservation primitive**, so reservation parity = no-oversubscription guardrail (admission control) + `balloon=0` + KSM off + **host-side confinement** (host services/IRQs restricted to housekeeping cores via firstboot) + `cpuunits` weighting; `sockets=1`, no cpulimit/rate/IO caps. **Per-VM `affinity` pinning is the escalation path**, gated on the Phase 2 jitter/latency soak | Proposed (legacy behavior **Confirmed**) | Team judgment: reservations mattered more than pinning; at ~2×1G edge throughput with zero oversubscription, exclusive pinning rarely earns its cost. `affinity` is root@pam-only (docs-confirmed) → if escalated, HostBaselineJob applies it root-context (plan §6 #31). |
| 22 | 2026-08-06 | Port-group naming standard `LOC_NAME_VlanNum` carries into Nautobot VLAN naming; VMInterface↔VLAN assignment replaces the port-group object | Proposed | The design job emits it; the audit job checks tag mappings the way the robot checked port groups. |
| 23 | 2026-08-06 | Per-VM serial consoles additionally exposed on host TCP ports (socat/ser2net units generated from the VM list, mgmt-VLAN-bound, firewalled to OpenGear/mgmt) | Proposed | Preserves the legacy telnet-to-host-port OpenGear workflow; `qm terminal` remains the native path. |
| 24 | 2026-08-06 | Snapshots/backups: native `qm snapshot`/`vzdump` (no extra tooling); thin provisioning retained (legacy zeroed-thick not carried — written policy sanctions thin) | Proposed | Thin storage is what makes native snapshots work; PBS deferred. |
| 25 | 2026-08-07 | L2 fabric is **always jumbo-capable**: Cat9300 `system mtu` raised (target 9198) as part of the standard switch build; Proxmox data path mirrors MTU 9000 | **Decided** | Rationale: capability up front means an MTU change is never needed later on an in-service stack (an outage-risk change class eliminated). Net-new sites get it in the template; existing sites get verified/raised proactively (confirm live-vs-reload on the fleet IOS-XE release before touching in-service stacks). Guests stay 1500 unless a profile opts in; PA HA2 (VLAN 907) is the likeliest jumbo consumer. |
| 26 | 2026-08-07 | **Primary deployment mode is net-new build** (lab as-built → ship); in-place conversion of live ESXi sites is the secondary case | **Decided** | The legacy robot's normal job is net-new. Consequences: new-site switch templates carry `mode active` + IP-hash + jumbo from day one (no flip runbook involved); the coordinated-flip and cutover-policy items (#13, #15) apply only to conversions; phase exit criteria emphasize the lab new-build path first. |
| 27 | 2026-08-07 | Boot storage = **ext4 + LVM-thin on the hardware-RAID boot volume**. No ZFS, no ARC reservation | **Decided** | Matches the ESXi single-presented-disk pattern; keeps the hardware mirror the team trusts; frees the 4–8 GiB ARC from the RAM budget; LVM-thin provides the snapshot capability (#24). **Corrected + RESOLVED 2026-08-09**: the fleet runs TWO RAID volumes — 128G boot + 1.92T data — and a live unit's udevadm capture (nfvlabspt1) showed both present the IDENTICAL `ID_MODEL=ThinkSystem_M.2_VD` (model filters can never discriminate) but ride **separate M.2 adapters**: boot at PCI `05:00.0`, data at `66:00.0`. Filter pinned to `ID_PATH: "pci-0000:05:00.0-ata-1.0"` (fleet-uniform hardware per #17); per-unit/revision verification = one `device-match` command in checklist §1. Degraded-mirror alerting still lab-verify (checklist §4). |
| 28 | 2026-08-07 | UEFI/BIOS build standard **provided** (legacy XCC BIOS tool): Thermal Performance; **Custom Mode**; C-States/C1E/Energy-Efficient-Turbo Disable; MONITOR/MWAIT Enable; Power Bias Platform Controlled → Maximum Performance; PCI 64-bit Auto; MM Config 3GB | **Confirmed** | Carried verbatim into `bmc/se350_bios.yaml` (`legacy_standard`); `proposed_additions` (COM1 redirect explicit, serial sharing off, Secure Boot off) pend sign-off — the tool doesn't touch those today. Kernel-cmdline C-state caps likely redundant now (BIOS disables C-states) — lab-confirm then simplify. Exact Redfish spellings verified by the discovery job before first PATCH. |
| 29 | 2026-08-07 | PA-VM: **PAN-OS 11.2.x** (environment's current supported version); KVM support posture **accepted** (Proxmox = standard KVM/QEMU; formally unqualified by PA); **standalone firewalls now, SCM near-term** — builder ships both templates, per-VM attribute selects | **Decided** | Remaining: fixed model vs flex credits (licensing), q35 machine-version matrix for 11.2 on PVE 9 `[lab-verify]`. |
| 30 | 2026-08-07 | Ubuntu jump host = **Desktop** → golden-template bake (no Desktop cloud image exists): build once with tooling + autolock-off baked, clone + cloud-init per deploy | **Decided** | Template build is manual first, Packer/CI later (plan Phase 5). |
| 31 | 2026-08-07 | 9800-CL sized for **Catalyst 9136 APs** → IOS-XE 17.18.x train (9136 requires ≥17.7) | **Decided** | Final release pick + template size from that release's install guide `[lab-verify]`. |
| 32 | 2026-08-07 | **nautobot-composer reachable from field nodes** — scenario-2 field redeploys pull images via `download-url` directly | **Decided** | Closes the image-delivery question for field ops; no upload-API fallback path needed. |
| 33 | 2026-08-07 | **No approval gating initially** — revisit at the Phase 4 field pilot | **Decided** | Twin-health pre-flight + dry-run preview remain the field guardrails; zeroes the 2.4→3.x approval-migration surface for now. |
| 34 | — | Switch-side captures via **RESTCONF** (if enabled on the 9300s): read-only `SwitchCaptureJob` pulling load-balance, system mtu, port-channel/access-port config, and CDP/LLDP neighbor tables (auto-builds the wiring map) over YANG | Proposed | Needs `restconf` + `ip http secure-server` + priv-15 creds, ACL-restricted to mgmt. Alternative: netmiko/SSH show-command capture — same data, screen-scraped. |
| 35 | 2026-08-07 | **Live API validation on lab PVE 9.2.2 (NUC)**: service-account role `NFVAutomation` + `svc-nfv@pve!deploy` proven sufficient for VM create (full profile), cloud-init, power ops, network staged/apply, `download-url`, `import-from` volume-ID; `affinity`/`hugepages`/arbitrary-path-import refuse even root's token ("only root can set") — root-context SSH is the only pinning path; **privsep token rights = intersection of token AND user ACLs (grant role to both)** | **Confirmed** | Service account left in place on the NUC for Phase 2 development. Content-deletion (image rotation) needs `Datastore.Allocate` — deliberate role gap for now. |
| 36 | 2026-08-07 | Image/template management via **core `SoftwareVersion`/`SoftwareImageFile`** (Nautobot 2.2+ core, present in 2.4): each golden image (Ubuntu jump host template, PAN-OS, C8000v, 9800-CL) = a SoftwareVersion under a dedicated Platform, with the ImageFile carrying filename/SHA256/size/`download_url` (nautobot-composer). `VirtualMachine.software_version` is deploy intent; **the Proxmox create call is generated** from VM fields (vcpus/memory/disk) + VMInterface↔VLAN assignments (`net` strings) + the platform profile (Git-synced config context) + the ImageFile (`download-url` → `import-from`). Status-gated promotion: build job registers new versions as *Staged*, a human flips to *Active*, the design job only selects Active. Provenance: version record links the template-build seed's git commit | Proposed | Kills hardcoded URLs/checksums in jobs; template rollout becomes a SoT operation (flip the Active version) with audit trail; EoS dates on versions give aged-template alerting. Note: `SoftwareImageFile`↔DeviceType M2M is device-side only — VM association rides the version FK; keep one image file per version (or `default_image`). |
| 37 | 2026-08-07 | **Build/deploy job split with templates-as-artifacts**: `BuildTemplateJob` (rare; designated build node; vendor cloud image + git-reviewed seed → sealed template → published to composer → Staged version) and `DeployVmJob` (constant; consumes published artifacts + identity-only native cloud-init). Vendor-qcow2 classes use a lighter `RegisterVendorImageJob`. Templates are disposable derived artifacts — sources of truth are the vendor base, the seed in git, and the profile | Proposed | See [image-lifecycle.md](image-lifecycle.md). Change = PR to the seed, never an edit to a golden VM; rollout/rollback = version status flips; CVE refresh = rebuild with unchanged seed. Deploy-time stays on native cloud-init keys (no snippets) — richer defaults baked via cloud.cfg.d in the seed. |
| 38 | 2026-08-08 | Lab-built sites are created **under their destination Location from day one** (no staging-location-then-move flow) | **Decided** | Simplifies the design job and keeps IPAM/VLAN records site-true during the as-built lab stage; the shipped unit changes physical place, not SoT identity. |
| 39 | 2026-08-08 | **Standing rule: SSoT-first** — always use Nautobot as the source when possible; when not, design for maximally normalized data. Operationalized: job inputs are object references; native fields over custom fields; standards in Git-synced config contexts; every consumer-read convention documented in [sot-data-contract.md](sot-data-contract.md) | **Decided** | Supersedes any job design that takes free-form values for data Nautobot holds. The v1 DeployCloudInitVm form fields become SoT reads in the device-driven deploy job. |
| 40 | 2026-08-08 | **Standing rule: desired-state data lives in the SoT and nowhere else, stored once.** Platform tunables = **Option A, Platform custom fields**: `day0_builder` (select; choice list maintained by the bootstrap job to match shipped builders exactly) + `machine_type` (text); immutable platform facts stay in code; bootstrap seeds create-only; deploy fails closed on missing/unknown tunables | **Decided** | Enables SoT-driven converge jobs (intent-vs-actual diff/apply; hot/restart/redeploy change classes) and JobHook-triggered drift reports — SoT as control plane. See [sot-data-contract.md](sot-data-contract.md). |
| 41 | 2026-08-09 | Bare-metal install delivery: **Redfish virtual media primary, PXE secondary** — both boot the SAME `prepare-iso` artifact, so the SoT spine (identity POST → answer service → `answer.toml` → firstboot → webhook) is delivery-agnostic and PXE is a delivery-layer option, not a fork | **Decided** | vmedia: no site-network dependency (needs only BMC reachability + the plain-HTTP ISO vhost), rides the validated dual-mode XCC client, production-proven pattern (OpenShift ZTP / OpenStack Ironic). PXE: **officially supported since PVE 9.2** (`prepare-iso --pxe --pxe-loader ipxe`) — the lab/staging path and the escape hatch for unlicensed BMCs; caveats: the full ~1.6–1.9 GB ISO travels into RAM (plan ≥4 GB), UEFI+HTTP is the reliable combo (legacy-BIOS iPXE fails at this payload size), requires DHCP/proxyDHCP services, and Secure-Boot-over-PXE is undemonstrated (the *installed* system is SB-signed since 8.1 either way). Generalizing beyond SE350: every major vendor licenses remote vmedia (iDRAC Enterprise / iLO Advanced / XCC Enterprise FoD / Supermicro OOB) and each has quirks — handled as the per-vendor vmedia-method layer in `bmc/` platform profiles (#14), the Ironic lesson. |
| 42 | 2026-08-09 | Install engine architecture: **generic core + delivery add-ons** (team direction). The core — answer service, prepared artifact, firstboot, state machine — is 100% vendor-neutral; vendor/BMC-specific code is confined to small delivery adapters selected by the DeviceType profile (`pve-nested` for the lab loop, `redfish-vmedia` for physical; PXE needs no adapter at all). | **Decided** | Honest framing of what vmedia adds over plain PXE in OUR model (all installs happen in the lab, #12/#26, where we control DHCP): for *delivery*, little — a blank disk falls through to PXE naturally and the answer service's serial allowlist makes a standing netboot service safe. The BMC's enduring value is NOT the media mount: it's remote power-on, **one-shot boot override for reinstalls** (a disk with an OS never falls through to PXE), Secure-Boot-clean CD boot, and the already-planned BIOS/discovery track — all standard Redfish, needed under either delivery. Primary-vs-secondary weighting (#41) stands for field/no-DHCP cases and may be re-weighed once both paths are lab-exercised. Built as the **L0 lab kit** (2026-08-09): answer service container, `InstallProxmoxNode` job, nested + vmedia adapters, prepared-ISO script — see [baremetal-install.md](baremetal-install.md). **Live-proven end-to-end on the lab NUC the same day** via the nested adapter: a blank VM (SMBIOS serial = SoT serial) auto-installed PVE 9.2 and self-registered its per-node API token into Nautobot Secrets with no human between; that token then authenticated back into the node with the full NFVAutomation privilege set. Lesson banked: `cert-fingerprint` must be pinned in BOTH the `[first-boot]` and `[post-installation-webhook]` answer sections for a self-signed answer-service cert (the installer's rustls client rejects `CaUsedAsEndEntity` otherwise). |
